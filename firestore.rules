rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow reads for all (app is behind Firebase Auth anyway)
    // Writes require authentication
    match /{document=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
